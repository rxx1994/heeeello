字符串去掉首尾的空格
strip()
如果只想删除左边
lstrip()
只想删除右边
rstrip()
括号里面什么都不写就是去空格，写了什么字符就去掉什么字符
！这个操作对字符串中间并不起作用，只管首尾

要想去中间的空格，可以用
.replace(' ','')
也可以用正则表达式
re.sub('\s+', ' ', str)
第一项是匹配模式
第二项是替换模式
第三项是处理的对象
在这个例子中，第二项是一个空格，并不是什么都没有
想要删去所有的空格，第二项里面什么都不要输入

lines = (line.strip() for line in f)
是一个可以用在读取数据的时候的代码
>>回去需要试一下2.11中的这段代码！
